// input
const min = parseInt(prompt("Enter a min value: "));
const max = parseInt(prompt("Enter a max value: "));

// generating a random number
const r = Math.floor(Math.random() * (max - min + 1)) + min;

// display
console.log(`Your random number is ${r}`);

